# iouring_cas_threadPool

假设现在是在开发高性能服务器的一个模块。这里要实现的功能接受高并发的请求，并且需要快速地处理这些请求。
自上而下，
第一，对于io部分，我们可以使用io_uring来提高性能，获取请求。io将任务下发给队列。
第二，关于任务队列的实现，可以使用环形缓冲区来存储任务，使用CAS操作来管理队列的头尾指针。
第三，线程池中的线程通过CAS操作来获取任务并执行，这样可以避免锁的使用，提高效率。